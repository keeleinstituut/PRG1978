## Example extraction and database creation code

Here is code for extracting a wider array of examples per construction and storing the data in a database format.

 - `methods/` stores python scripts
 - `notebooks/` stores notebook files with implementation
